# GTR7 Home profile

`default.nix` 是 `nixos-gtr7` 的完整 Home Manager 入口，启用 GUI/媒体工具、录播容器、cp-guard、代理、Zsh、LSP/AI Neovim、Alacritty、WezTerm 和 Attic watch-store。

这是新用户态功能的首要试用 profile。系统级桌面、音频和虚拟化配置在 `host/nixos-gtr7/` 与 `os/collections/gtr.nix`。
